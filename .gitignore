# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Extraction intermediates (raw IES/CSV dumps — not for distribution, regenerate locally)
data-src/
__pycache__/
*.pyc

# Tooling caches
.serena/

# Local agent settings (contains personal tokens)
.claude/*
# ...ただし共有したい手順書（スキル）は追跡する（親ディレクトリを除外すると
# 再包含できないので `.claude/` ではなく `.claude/*` にしてある）
!.claude/skills/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
